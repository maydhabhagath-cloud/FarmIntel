# FarmIntel smoke tests

The Playwright suite in `smoke.spec.js` covers the core demo acceptance flows: page load, navigation, crop-lot validation/state updates, market intelligence, Sell Smart, buyer/offers views, and offer acceptance.

Run locally with `npm install`, `npx playwright install`, `npm run start` in one terminal, and `npm test` in another.
